# E001: Derived values remain stale in cache

[Documentation](../index.md) → [Errata](index.md) → E001 · [Русский](../../ru/errata/E001.md)

**Status:** Merged in [PR #6](https://github.com/ViacheslavMezentsev/stm32-cmake-yml/pull/6); release not established. **Baseline:** `0.9.2 @ f8ef5200fc7a4a96d6f3fe9111afb8f8825474b0`.

[`mcu`](../reference/0.9.2/project.md#mcu), [`heap_size`](../reference/0.9.2/linker.md#heap-size), [`stack_size`](../reference/0.9.2/linker.md#stack-size)

## Reproduction

Change the MCU or heap/stack profile and configure again in the same build tree.

## Expected and actual behavior

Expected: new MCU/define and new .ld values. Actual: set(... CACHE ...) without FORCE preserves initial MCU, HEAP_SIZE and STACK_SIZE.

## Workaround and compatibility

Use a separate fresh build tree per profile. After the fix use STM32_YML_OVERRIDE_*; direct -DMCU/-DHEAP_SIZE/-DSTACK_SIZE no longer take precedence.

## Evidence and fix

Reproduced in configuration tests. Regressions `configure.reconfigure-memory`
and `configure.reconfigure-mcu` are in the fix branch's
[manifest](../../../tests/cases.json); they were absent from the original 0.9.2 baseline.

[Source](https://github.com/ViacheslavMezentsev/stm32-cmake-yml/blob/f8ef5200fc7a4a96d6f3fe9111afb8f8825474b0/cmake/stm32_yml_config.cmake).

Fix: `858013a7f31a674a91149ea3a68a5200498d92cc`, branch `codex/configure-profile-fixes`.
Merge: `5711ec6`. Status checked 2026-09-23. A separate release is not confirmed.
